
version: 2

models:
  - name: int_tpch_location
    tags:
      - location
    columns:
      - name: nation_key
        tests:
          - unique
          - not_null
      - name: region_key
        tests:
          - not_null

