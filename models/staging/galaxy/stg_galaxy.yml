version: 2

models:
  - name: stg_galaxy_enigma
    description: staging layer for customers data
    columns:
      - name: last_update
        tests:
          - not_null
  - name: stg_galaxy_enigma_2
    description: staging layer for customers data
    columns:
      - name: last_update
        tests:
          - not_null